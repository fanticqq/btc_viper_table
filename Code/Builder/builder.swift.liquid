//
//  {{ prefix }}{{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit

typealias {{ module_info.name }}ModuleBox = (viewController: {{ module_info.name }}ViewController, input: {{ module_info.name }}ModuleInput, output: {{ module_info.name }}ModuleOutput)

class {{ module_info.name }}Builder {

    func createModule() -> {{ module_info.name }}ModuleBox {
        let dataDisplayManager = {module_info.name}}DataDisplayManager()
        let viewController = {{ module_info.name }}ViewController(style: .grouped, datadisplayManager: datadisplayManager)
        
        let presenter = {{ module_info.name }}Presenter()
        presenter.view = viewController

        let interactor = {{ module_info.name }}Interactor()
        interactor.output = presenter

        presenter.interactor = interactor
        viewController.output = presenter
        
        return (viewController, presenter, presenter)
    }
}
